import styled from "styled-components";
export const Pn=styled.h3`
    font-weight: normal;
    font-size: var(--xs-font);
    text-align: center;
    margin: auto;
    margin-top: 2rem;
    color: var(--whitePen);
    a{
      text-decoration: underline;
      &:hover{
        color: var(--bluePen);
      }
    }

`